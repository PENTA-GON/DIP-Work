clear;close all;clc;

nbins = 10;

I_A = imread('77.jpg');
I_B = imread('75.jpg');

[hist_A, bin_A] = imhist(I_A, nbins);
[hist_B,  = imhist(I_B, nbins);

%% http://jccaicedo.blogspot.sg/2012/01/histogram-intersection.html 
% a = size(hist_A, 1);
% b= size(hist_B, 1);
% K = zeros(a, b);
% L = zeros(a, b);
% for i= 1 : a
%      
%    K(i,:) = 0.5 * sum(hist_A + hist_B - abs(hist_A - hist_B));
%    L(i,:) = min(hist_A, hist_B);
%     
% end
